# Server Configuration
NODE_ENV=development
PORT=8080
API_VERSION=v1

# Database Configuration (SQL SERVER LOCAL)
DB_HOST=localhost
DB_PORT=1433
DB_NAME=easybody_db
DB_USER=sa
DB_PASSWORD=12345
DB_DIALECT=mssql

# JWT Configuration (LOCAL AUTH - không cần AWS Cognito)
JWT_SECRET=easybody_super_secret_key_2024_change_in_production
JWT_EXPIRES_IN=24h

# CORS Configuration
CORS_ORIGIN=http://localhost:3000,http://localhost:5173

# Logging
LOG_LEVEL=debug

# Pagination
DEFAULT_PAGE_SIZE=20
MAX_PAGE_SIZE=100

# Geo Search (SQL Server spatial data)
DEFAULT_RADIUS_KM=10
MAX_RADIUS_KM=100

# Image Upload (local storage tạm thời)
MAX_FILE_SIZE_MB=10
ALLOWED_IMAGE_TYPES=image/jpeg,image/png,image/gif,image/webp
